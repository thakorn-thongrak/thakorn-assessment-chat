# LINE Developers Console > Messaging API channel > Basic settings
LINE_CHANNEL_SECRET=

# LINE Developers Console > Messaging API channel > Messaging API tab
# Issue a "Channel access token (long-lived)"
LINE_CHANNEL_ACCESS_TOKEN=

# LINE Official Account's Basic ID (the @handle people add as a friend).
# Public info, not a secret — shown in the UI as an "add friend" link.
NEXT_PUBLIC_LINE_OA_ID=@831ltgzv
